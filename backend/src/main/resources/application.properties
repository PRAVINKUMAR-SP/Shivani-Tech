spring.application.name=recruitment
server.port=8080

# Database Configuration (PostgreSQL Neon)
spring.datasource.url=${DB_URL:jdbc:postgresql://ep-super-glade-auxkjksh-pooler.c-10.us-east-1.aws.neon.tech/neondb?sslmode=require}
spring.datasource.username=${DB_USER:neondb_owner}
spring.datasource.password=${DB_PASSWORD:npg_Ml34PazXAhYG}
spring.datasource.driver-class-name=org.postgresql.Driver

# Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# JWT Secret (In production, use a strong environment variable)
jwt.secret=${JWT_SECRET:aVeryLongSecretKeyThatShouldBeAtLeast256BitsLongForHmacSha256}
jwt.expiration=86400000

spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
